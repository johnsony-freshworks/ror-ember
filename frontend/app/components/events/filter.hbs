<form class="search-container d-flex justify-content-end py-4">
    {{!-- <div>
        <!-- Button trigger modal -->
        <button type="button" class="btn btn-secondary py-2 search-item" data-bs-toggle="modal" data-bs-target="#daterangepicker">
            JAN 29, 2021 08:07 PM - JAN 30, 2021 08:07 PM
        </button>
        <!-- Modal -->
        <div class="modal fade" id="daterangepicker" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="daterangepickerLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="daterangepickerLabel">Date Range</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <div class="mb-3">
                            <label for="recipient-name" class="col-form-label">Start:</label>
                            <input type="datetime-local" class="form-control" id="recipient-name">
                        </div>
                        <div class="mb-3">
                            <label for="recipient-name" class="col-form-label">End:</label>
                            <input type="datetime-local" class="form-control" id="recipient-name">
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary">Submit</button>
                    </div>
                </div>
            </div>
        </div>
    </div> --}}
    <div class="d-flex align-items-center">
        <label class="text-nowrap me-3" for="pagecount">items per page</label>
        <select id="pagecount" name="newType" class="form-select py-2 search-item" onchange={{action (mut @size) value="target.value"}}>
            {{#each @pageCounts as |size|}}
                <option selected={{if (equal size @size) 'true'}}>{{size}}</option>
            {{/each}}
        </select>

    </div>
</form>